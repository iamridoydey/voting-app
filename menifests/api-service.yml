apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: voting-app
spec:
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000 # Targetting container
  type: LoadBalancer